# API Types

This folder contains the API types for the app. Put feature-focused API types here, for example:

- `auth.types.ts`
- `user.types.ts`
- `example.types.ts`

Each API type should export types that encapsulate API types and keep request/response details out of components.