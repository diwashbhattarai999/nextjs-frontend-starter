
# Query Options

This folder contains the query options for the app. Put feature-focused query options here, for example:

- `auth.query-options.ts`
- `user.query-options.ts`
- `example.query-options.ts`

Each query option should export functions that encapsulate query options and keep request/response details out of components.
