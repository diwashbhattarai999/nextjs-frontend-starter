
# Services

This folder contains the API layer for the app. Put feature-focused service modules here, for example:

- `auth.service.ts`
- `user.service.ts`
- `billing.service.ts`

Each service should export functions that encapsulate API calls and keep request/response details out of components.
