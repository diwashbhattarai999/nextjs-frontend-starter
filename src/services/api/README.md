# API Services

This folder contains the API layer for the app. Put feature-focused API modules here, for example:

- `auth.api.ts`
- `user.api.ts`
- `example.api.ts`

Each API module should export functions that encapsulate API calls and keep request/response details out of components.